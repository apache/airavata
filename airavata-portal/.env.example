# Airavata API (server-side; portal proxies /api/v1/* to this URL)
API_URL=http://localhost:8090

# Keycloak (no login form in app; redirect goes to Keycloak)
KEYCLOAK_ISSUER=http://localhost:18080/realms/default
KEYCLOAK_CLIENT_ID=pga
KEYCLOAK_CLIENT_SECRET=

# NextAuth (must match the URL you open in the browser, e.g. http://localhost:3000; avoids Keycloak 400 on login)
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
