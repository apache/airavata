# Init command profile: minimal context, DataSource + Flyway only.
# Overrides application.properties to avoid loading Thrift/REST/Orchestrator stack.
airavata.services.thrift.enabled=false
services.thrift.enabled=false
services.rest.enabled=false
