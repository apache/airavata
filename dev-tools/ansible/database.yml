---
# Gather facts on the following
- hosts: api-orch
- hosts: gfac

- hosts: database
  tags: mysql , airavata
  roles:
    - env_setup
    - role: database
      become: yes
      become_user: "{{user}}"

...
