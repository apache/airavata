---

# Assumption here is that develop has already built the Airavata distribution locally
- hosts: local
  vars:
      server_deployment_dir: "{{ deployment_dir }}"
      server_name: all
  roles:
      - airavata_deploy
