apiVersion: v1

kind: Service

metadata:

  name: airavata-all
  
  labels:
    name: airavata-all
    app: airavata
 
spec:

  type: NodePort

  ports:

  - port: 8080

    nodePort: 30002

    protocol: TCP

  selector:
    name: airavata-all
    app: airavata


